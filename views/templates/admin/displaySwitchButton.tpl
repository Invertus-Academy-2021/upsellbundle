<label class="switch">
    <input type="checkbox" class="customInput"
           {if $activated == 1}checked{/if}
           id="{$id_chained}" data-target="{$id_product}" data-product="{$id_chained}">
    <span class="slider round"></span>
</label>